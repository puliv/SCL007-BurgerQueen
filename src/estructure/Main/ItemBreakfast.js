import React, { Component } from "react"
import { connect } from "react-redux"

class ItemBreakfast extends Component {


    render() {
        return (
            <button></button>
        )
    }
}

export default connect()(ItemBreakfast)